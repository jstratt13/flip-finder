node_modules/
dist/
.DS_Store

# Cloudflare local state
.wrangler/

# Secrets and local config. Real values live in `wrangler secret put` and in
# your own .env files, never in the repo.
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example

# One-off research data (eBay listing samples); never committed — the repo is public.
research/
