node_modules/
dist/
.DS_Store

# Cloudflare local state
.wrangler/

# Secrets and local config. Real values live in `wrangler secret put` and in
# your own .env files, never in the repo.
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example

# Research data stays local: eBay listing samples and anything derived from them
# (result titles, judged samples). The repo is public; the scripts are tracked.
research/ebay-sample-*/
research/out/
